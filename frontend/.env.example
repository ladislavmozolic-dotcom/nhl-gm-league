# Copy to .env on the server and fill in real values. NEVER commit the real .env.

# Your domain (must point to the server's IP — see DEPLOY.md step 3).
DOMAIN=liga.tvojadomena.sk

# Postgres — pick a strong password.
POSTGRES_USER=profinhl
POSTGRES_PASSWORD=CHANGE-ME-to-a-long-random-string
POSTGRES_DB=profinhl

# The app connects to the `db` service by name. Keep user/pass/db in sync with the
# three lines above.
DATABASE_URL=postgresql://profinhl:CHANGE-ME-to-a-long-random-string@db:5432/profinhl?schema=public

# Secret for the /api/cron/advance-day endpoint — the 20:30 Europe/Bratislava
# automatic daily sim trigger (see DEPLOY.md section 10b). Generate with:
#   openssl rand -hex 32
CRON_SECRET=CHANGE-ME-generate-with-openssl-rand-hex-32

# Session signing: generate with openssl rand -hex 32. Never use a sample secret.
# Changing this value signs out all GMs.
AUTH_SECRET=

# Existing password-hash salt. KEEP the old value when upgrading an existing DB.
# If the old deployment never set AUTH_SALT, it used profinhl-salt.
# For a NEW database only, generate a random value with openssl rand -hex 32.
AUTH_SALT=
