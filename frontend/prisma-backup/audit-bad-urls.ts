import { PrismaClient } from "@prisma/client";

.player.count({
    where: {
      photoUrl: {
        startsWith: "<a href=",
    startsWith: "<a href=",
      },
    },
  });

  consoleFrozenPoolUrls: frozenPoolUrls,
  });
}

main()
  .catch(console.error)
  .finally(async () => {
    await prisma.$disconnect();
  });